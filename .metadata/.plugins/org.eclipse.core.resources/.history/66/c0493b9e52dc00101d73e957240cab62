import java.util.Arrays;

public class SmashWords {

	public static String smash(String... words) {
		StringBuilder sb = new StringBuilder();
		System.out.println(Arrays.stream(words).toString());
		return "";
  }
}