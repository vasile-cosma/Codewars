package clases;

import java.util.Arrays;

public class Main {

	public static void main(String[] args) {
		char[] caracteres = String.valueOf(num).toCharArray();
		Arrays.sort(caracteres);
		System.out.println(caracteres);
		return (Integer.parseInt(caracteres.toString()));

	}

}
