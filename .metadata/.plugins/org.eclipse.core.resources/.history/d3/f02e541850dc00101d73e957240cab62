import java.util.Arrays;
import java.util.stream.IntStream;

public class FindOutlier {
  static int find(int[] integers) {
	  IntStream par = Arrays.stream(integers).filter(x -> x%2==0);
	  int impar = 0;
	  
  }
}