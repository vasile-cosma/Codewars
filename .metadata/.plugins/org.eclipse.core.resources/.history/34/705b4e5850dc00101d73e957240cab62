import java.util.Arrays;

public class FindOutlier {
  static int find(int[] integers) {
	  int par = (int)Arrays.stream(integers).filter(x -> x%2==0).count();
	  if (par>1)
		  return Arrays.stream(integers).findFirst(x -> x%2!=0)
	  else
	  
  }
}