import java.util.Arrays;

public class FindOutlier {
  static int find(int[] integers) {
	  int par = 0;
	  int impar = 0;
	  Arrays.stream(integers).forEach(x -> if(x%2==0) par++ else impar++);
  }
}