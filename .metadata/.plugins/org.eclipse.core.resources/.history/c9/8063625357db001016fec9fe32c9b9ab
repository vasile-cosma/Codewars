package clases;

public class DescendingOrder {
	public static int sortDesc(final int num) {
		int numFinal;
		
		StringBuilder sb = new StringBuilder(Integer.toString(num));
		StringBuilder numCadena = sb.reverse();
		numFinal = Integer.parseInt(sb.reverse());
		
		
		return numFinal;
	  }
}
